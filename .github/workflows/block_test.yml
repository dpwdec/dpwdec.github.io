name: block_teste
on: [push]
jobs:
  check:
    name: make a check
    outputs:
      result: ${{ steps.set_return.outputs.result }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: make a result
        id: set_return
        run: echo "::set-output name=result::true"
  
  do_something:
    name: do something if true
    needs: check
    if: needs.check.outputs.result == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: do again
        run: ls -a